이 파일을 시작으로 주석처리는 영어로 바꾼다.
영어랑 익숙해져야한다고 생각하기에....

이 문제는 투 포인터 알고리즘을 사용하여 푸는 문제이다.
해당 알고리즘은 사용하기 전 정렬을 요구한다.

하지만 일반적인 O(n^2)를 사용하면 시간 초과가 발생하니
O(n log n)의 sort 함수를 사용해준다. (직접 구현하지 못한다는 것이 서글플 다름)

그 후, 순서쌍을 구하는 것은 O(n)의 투 포인터 알고리즘.